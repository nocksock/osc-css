<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="{{ package.description }}" />
    <link rel="apple-touch-icon" sizes="180x180" href="{{ '/apple-touch-icon.png' | url }}" />
    <link rel="icon" type="image/png" sizes="32x32" href="{{ '/favicon-32x32.png' | url }}" />
    <link rel="icon" type="image/png" sizes="16x16" href="{{ '/favicon-16x16.png' | url }}" />
    <title>{{ title or package.title }}</title>
    <link rel="stylesheet" href="{{ '/assets/style.css' | url }}" />
  </head>

  <body>
    <div class="stage min-h-svh">
      {{ content | safe }}
    </div>

    <script src="{{'/assets/main.mjs' | url}}"></script>
  </body>
</html>
